services:
  lb-dashboard:
    build: .
    ports:
      - "8501:8501"
    volumes:
      - lb-cache:/app/cache
    environment:
      - LB_CACHE_DIR=/app/cache
      - LB_CACHE_TTL=1
    restart: unless-stopped

volumes:
  lb-cache:
